playwright>=1.44
httpx[http2]>=0.27
jsbeautifier>=1.15
textual>=0.60.0
pyotp>=2.9
aiohttp>=3.9
ldap3>=2.9
impacket>=0.11.0  # NTLM hash capture via smb_sink for sql_probe xp_dirtree coercion
msgpack>=1.0      # optional: Metasploit RPC (msf_ingest) — install only if you use [msf].enabled
psycopg[binary]>=3.1  # optional: direct-PG fallback for msf_ingest — install only if you use [msf].enabled
